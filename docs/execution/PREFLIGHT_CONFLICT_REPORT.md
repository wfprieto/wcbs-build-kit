# WCBS Surpass-Superpowers Preflight Conflict Report

## Decision

Proceed with environment-controllable remediation only. The user authorized
implementation, but no code or fixture can substitute for a clean authenticated
runtime, paid evaluation credential, independent human scoring, protected-branch
authority, or external pilot evidence.

## Resolved implementation path

1. Preserve the published baseline and the dirty historical checkout.
2. Consolidate the V2 native-adapter, transport, release-artifact, and
   evaluation-integrity work on one branch.
3. Repair any release-gate regression before adding new capability.
4. Keep runtime proof and behavioral superiority as external, evidence-gated
   phases rather than treating fixtures as a pass.

## External blockers and owners

| Blocker | Why agents cannot close it here | Required owner action |
|---|---|---|
| Clean-session runtime proof | No authenticated vendor runtime session is available. | Run the documented native acceptance procedure for the selected runtime. |
| Gate 0C execution | Needs a committed exact CLI/model identity, vendor loader, isolated credential, and independent scoring. | Authorized evaluation owner preregisters and executes the locked protocol. |
| GitHub merge/tag/hosted CI | This workspace has no protected-branch or GitHub release authority. | Repository maintainer pushes the reviewed branch and verifies required CI. |
| External pilot and maturity | Requires time and consenting outside teams. | Product owner runs the consented pilot and records redacted outcomes. |

No blocker is downgraded or bypassed. The final release verdict remains
`PARTIAL` until the corresponding external evidence exists.
